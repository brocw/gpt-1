# PyTorch ROCm build — install with:
# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/rocm7.2
torch==2.12.0+rocm7.2
pytorch-triton-rocm==3.5.1
numpy==2.4.6
gguf==0.19.0
